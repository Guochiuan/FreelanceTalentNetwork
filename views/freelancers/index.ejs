<% layout('layouts/boilerplate')%>
    <div id="cluster-map"></div>
    <div class="container">
        <h1>All Freelancers</h1>

        <% for (let freelancer of freelancers){%>
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">
                        <%if(freelancer.images.length) {%>
                            <img class="img-fluid" alt="" src="<%=freelancer.images[0].url%>">
                            <% }else {%>
                                <img class="img-fluid" alt=""
                                    src="https://res.cloudinary.com/douqbebwk/image/upload/v1600103881/YelpCamp/lz8jjv2gyynjil7lswf4.png">
                                <% } %>
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= freelancer.title %>
                            </h5>

                            <p class="card-text">
                                <%= freelancer.description %>
                            </p>
                            <p class="card-text">
                                <small class="text-muted">
                                    <%= freelancer.location%>
                                </small>
                            </p>
                            <a class="btn btn-primary" href="/freelancers/<%=freelancer._id%>">View
                                <%=freelancer.title%>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            <% }%>
    </div>
    <script>
        const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
        const freelancers = { features: <%- JSON.stringify(freelancers) %>}
    </script>

    <script src="/javascripts/clusterMap.js"></script>